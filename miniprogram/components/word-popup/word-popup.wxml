<view wx:if="{{show}}" class="popup-mask" bindtap="handleMaskTap">
  <view class="popup-card" bindtap="handleCardTap">
    <button class="popup-close" plain="true" hover-class="none" bindtap="handleClose">×</button>
    <text class="popup-word">{{word}}</text>
    <text class="popup-ipa">{{phonetic || '加载中...'}}</text>
    <text class="popup-hint">点击播放，模仿节奏与语调</text>
    <button class="popup-play" bindtap="handlePlay" loading="{{loading}}" disabled="{{loading}}">
      <text class="popup-play-icon">🔊</text>
      播放
    </button>
  </view>
</view>
